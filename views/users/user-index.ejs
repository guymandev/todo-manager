<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs', {title: 'Users'}) %>
    <link rel="stylesheet" href="/styles/user-index.css">
</head>

<body class="radial-blue">
    <%- include('../partials/nav.ejs') %>

    <section class="hero">
        <h2>List of Users</h2>
    </section>
    <br>    
    <a class="new-user-button" href="/users/new">Create New User</a>
    <br>
    <main>
        <section class="listings">
            <ul>
                <% for (let user of users) { %>
                <li class="user-index-item">        
                    <div>
                        <a class="user-name" href="/users/<%= user._id %>"><%= user.name %></a>
                    </div>
                    <div class="users-buttons-group">
                        <a class="button" href="/users/<%= user._id %>/edit">Edit</a>
                        <form method="POST" action="/users/<%= user._id %>/delete?_method=DELETE">
                            <input class="delete-button" value="Delete" type="submit"/>
                        </form>                                                
                    </div>
                </li>
                <% } %>
            </ul>
        </section>
    </main>


</body>

</html>